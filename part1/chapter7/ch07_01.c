#include <stdio.h>

int main() {
    int values[3];
    values[0] = 52;
    values[1] = 38;
    values[2] = 100;

    printf("配列の文字 = %d %d %d \n",
            values[0], values[1], values[2]);

    return 0;
}
